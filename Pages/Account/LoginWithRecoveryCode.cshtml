@page
@model LoginWithRecoveryCodeModel
@{
    ViewData["Title"] = "Weryfikacja kodu odzyskiwania";
}

<h2>@ViewData["Title"]</h2>
<hr />
<p>
    Zażądano zalogowania się przy użyciu kodu odzyskiwania. Ten login nie zostanie zapamiętany, dopóki nie podasz
    kod aplikacji uwierzytelniającej przy logowaniu lub nie wyłączysz 2FA i ponownie się zalogujesz.
</p>
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="All" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="Input.RecoveryCode"></label>
                <input asp-for="Input.RecoveryCode" class="form-control" autocomplete="off" />
                <span asp-validation-for="Input.RecoveryCode" class="text-danger"></span>
            </div>
            <button type="submit" class="btn btn-default">Zaloguj</button>
        </form>
    </div>
</div>
 
 @section Scripts {
     @await Html.PartialAsync($"_ValidationScriptsPartial")
 }