@page
@model ExternalLoginModel
@{
    ViewData["Title"] = "Rejestracja";
}

<h2>@ViewData["Title"]</h2>
<h4>Powi¹¿ swoje konto @Model.LoginProvider.</h4>
<hr />

<p class="text-info">
    Uda³o Ci siê uwierzytelniæ za pomoc¹ <strong>@Model.LoginProvider</strong>.
    WprowadŸ poni¿ej adres e-mail tej witryny i kliknij przycisk "Rejestruj", aby zakoñczyæ
    logowanie.
</p>

<div class="row">
    <div class="col-md-4">
        <form asp-page-handler="Confirmation" asp-route-returnUrl="@Model.ReturnUrl" method="post">
            <div asp-validation-summary="All" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="Input.Email"></label>
                <input asp-for="Input.Email" class="form-control" />
                <span asp-validation-for="Input.Email" class="text-danger"></span>
            </div>
            <button type="submit" class="btn btn-default">Rejestruj</button>
        </form>
    </div>
</div>

@section Scripts {
    @await Html.PartialAsync($"_ValidationScriptsPartial")
}
